# Point at the FastAPI backend. Switch to a deployed URL for production.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# When true the frontend reads /frontend/lib/mockData.js directly instead of
# calling the backend. Use this while iterating on UI so you don't need the
# backend or any real API keys running.
NEXT_PUBLIC_USE_MOCK=true
