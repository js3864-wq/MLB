ANTHROPIC_API_KEY=
CJ_API_KEY=
CJ_API_EMAIL=
SUPABASE_URL=
SUPABASE_KEY=

# When MOCK_MODE=true the pipeline uses fixtures in backend/mock_data
# instead of calling pytrends / CJ / Claude / Supabase. Use this while
# iterating on the frontend so we don't burn API calls.
MOCK_MODE=true
